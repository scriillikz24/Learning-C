# 1. Ignore all compiled binaries (files with no extension)
*
!*/
!*.*

# 2. Specifically allow Makefiles (they have no dot)
!Makefile

# 3. Specifically ignore object files and common C junk
*.o
*.out
*.exe
